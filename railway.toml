# JARVIS Railway Deployment — Backend (FastAPI)
# Frontend usa web-next/railway.toml (Root Directory = /web-next en Railway dashboard)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.backend"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
